# ─── Development & test dependencies ────────────────────────────────────────
-r requirements.txt

# --- Testing ---
pytest~=8.3.0
pytest-asyncio~=0.24.0
pytest-cov~=6.0.0
httpx==0.27.2                       # pinned: anthropic 0.39 incompatible with 0.28+
aiosqlite~=0.20.0                  # lightweight DB for unit tests

# --- Quality / lint / format ---
ruff~=0.8.0                        # linter + formatter (replaces black/isort/flake8)
mypy~=1.13.0
types-redis~=4.6.0

# --- Tooling ---
pip-tools~=7.4.0                   # compile pinned lockfiles
pre-commit~=4.0.0
